/**
 * Deprecated umbrella package.
 *
 * This repo is split into scoped packages. Keep this entrypoint as a thin re-export
 * to avoid breaking older `oimdb` consumers that still import from the root package.
 */
export * from '@oimdb/core';




