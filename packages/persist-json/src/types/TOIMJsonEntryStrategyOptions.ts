/**
 * Options for the JSON entry strategy: the key under which a resource's
 * snapshot is stored in the persistor's plain `data` object.
 */
export type TOIMJsonEntryStrategyOptions = {
    storageKey: string;
};
